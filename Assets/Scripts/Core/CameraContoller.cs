using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraContoller : MonoBehaviour
{
    Vector2 target;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //target = GameManager.instance.playerCon.transform.position;
        //transform.position = new Vector3(0, 0, -10) + (Vector3)Vector2.Lerp(transform.position, target, 0.01f);
    }
}
